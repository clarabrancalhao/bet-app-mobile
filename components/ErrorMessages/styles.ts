import { Text } from 'react-native'
import styled from 'styled-components'

export const ErrorText = styled(Text)`
  padding-top: 10px;
  color: red;
  font-size: 14px;
  font-weight: 600;
  margin-left: 24px;
`
